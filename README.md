# cloud-stream-rabbit-sample

sample producer/consumer for spring cloud streams with rabbit-mq binder

use `curl -X POST -d{} http://localhost:8080/greet/hello` on producer side and watch the results.
